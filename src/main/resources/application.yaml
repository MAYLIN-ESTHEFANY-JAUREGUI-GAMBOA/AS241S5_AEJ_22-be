# Configuración de MongoDB
spring:
  application:
    name: SistemaIAs
  data:
    mongodb:  
      uri: ${MONGODB_URI} 

# Configuración del servidor
server:
  port: ${PORT} 8085

# Configuración de RapidAPI
rapidapi:
  chat:
    url: https://chatgpt-42.p.rapidapi.com/chat
    key: 1db400e164msh1e008a7113ead2fp1f3b4cjsn01b2dc50873b
    host: chatgpt-42.p.rapidapi.com
  ghibli:
    url: ${GHIBLI_URL}
    key: ${GHIBLI_KEY} 
    host: ${GHIBLI_HOST} 

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    pe.edu.vallegrande.SistemaIAs: DEBUG
    org.springframework.web.reactive.function.client: DEBUG
